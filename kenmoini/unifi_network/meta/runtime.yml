#SPDX-License-Identifier: MIT-0
---
# Collections must specify a minimum required ansible version to upload
# to galaxy
requires_ansible: '>=2.9.10'

# Content that Ansible needs to load from another location or that has
# been deprecated/removed
# plugin_routing:
#   action:
#     redirected_plugin_name:
#       redirect: ns.col.new_location
#     deprecated_plugin_name:
#       deprecation:
#         removal_version: "4.0.0"
#         warning_text: |
#           See the porting guide on how to update your playbook to
#           use ns.col.another_plugin instead.
#     removed_plugin_name:
#       tombstone:
#         removal_version: "2.0.0"
#         warning_text: |
#           See the porting guide on how to update your playbook to
#           use ns.col.another_plugin instead.
#   become:
#   cache:
#   callback:
#   cliconf:
#   connection:
#   doc_fragments:
#   filter:
#   httpapi:
#   inventory:
#   lookup:
#   module_utils:
#   modules:
#   netconf:
#   shell:
#   strategy:
#   terminal:
#   test:
#   vars:

# Python import statements that Ansible needs to load from another location
# import_redirection:
#   ansible_collections.ns.col.plugins.module_utils.old_location:
#     redirect: ansible_collections.ns.col.plugins.module_utils.new_location


action_groups:
  fullset:
    - acl_rules_info
    - acl_rule_info
    - adopt_device
    - adopted_devices_info
    - application_info
    - client_info
    - clients_info
    - countries_info
    - device_action
    - device_info
    - device_statistics
    - device_tags_info
    - dpi_applications_info
    - dpi_categories_info
    - firewall_zone_info
    - firewall_zones_info
    - hotspot_voucher_info
    - hotspot_vouchers_info
    - network_info
    - network_references
    - networks_info
    - pending_devices
    - port_action
    - radius_profiles_info
    - site_info
    - site_to_site_tunnels_info
    - traffic_matching_list_info
    - traffic_matching_lists_info
    - vpn_servers_info
    - wan_interfaces_info
    - wifi_broadcast_info
    - wifi_broadcasts_info